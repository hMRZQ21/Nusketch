import 'dart:ui';

class CustomColors {
  // can add colors here
  // try if you can use this:
  // static final Color testColor = Color.fromHex('#0f0f0f');
  // for me it does not work ~ Willie

  static const Color mainColor = Color.fromRGBO(245, 245, 245, 1);
  static const Color lavender = Color.fromRGBO(230, 230, 250, 1);
  static const Color beige2 = Color.fromRGBO(238, 213, 176, 1);
  static const Color lightBlue = Color.fromRGBO(208, 225, 232, 1);
  static const Color lightBeige = Color.fromRGBO(255, 231, 204, 1);
  static const Color customDarkBeige = Color.fromRGBO(248, 203, 166, 1);
}
